{% extends '::base.html.twig' %}
{% block title %}Administraci&oacute;n - Parametros del Sistema{% endblock %}
{% block body -%}
<div class="page-header">
  <h1>Parametros del Sistema<small>Listado</small>
  </h1>
</div>

{% include '::VariosSistema/mensajes_flash.html.twig' %}

<div class="col-xs-offset-1 col-xs-11 col-md-offset-1 col-md-9">
<table id="system-parameters" class="table table-hover table-condensed" style="margin-bottom: 0px;">
    <thead>
        <tr>
            <th>Parametro</th>
            <th>Estatus</th>
            <th>Acciones</th>
        </tr>
    </thead>
    <tbody>
      <tr>
        <td>Conversión onza troy a gramos</td>
        <td>Activo</td>
        <td>
          <a title="Ver" style="text-decoration: none;" href="{{ path('parametros_show', { 'id': 1 }) }}">
                      {% image 'assets/self/images/ver.png' %}
                        <img src="{{ asset_url }}" alt="Ver" />
                      {% endimage %}
          </a>
          <a title="Ver" style="text-decoration: none;" href="{{ path('parametros_edit', { 'id': 1 }) }}">
                      {% image 'assets/self/images/editar.png' %}
                        <img src="{{ asset_url }}" alt="Editar" />
                      {% endimage %}
          </a>
        </td>
      </tr>
      <tr>
        <td>Margen de Ganancia: Compra</td>
        <td>Activo</td>
        <td>
          <a title="Ver" style="text-decoration: none;" href="{{ path('margenes_ganancia_show', { 'id': 1 }) }}">
                      {% image 'assets/self/images/ver.png' %}
                        <img src="{{ asset_url }}" alt="Ver" />
                      {% endimage %}
          </a>
          <a title="Ver" style="text-decoration: none;" href="{{ path('margenes_ganancia_edit', { 'id': 1 }) }}">
                      {% image 'assets/self/images/editar.png' %}
                        <img src="{{ asset_url }}" alt="Editar" />
                      {% endimage %}
          </a>
        </td>
      </tr>
      <tr>
        <td>Margen de Ganancia: Venta</td>
        <td>Activo</td>
        <td>
          <a title="Ver" style="text-decoration: none;" href="{{ path('margenes_ganancia_show', { 'id': 2 }) }}">
                      {% image 'assets/self/images/ver.png' %}
                        <img src="{{ asset_url }}" alt="Ver" />
                      {% endimage %}
          </a>
          <a title="Ver" style="text-decoration: none;" href="{{ path('margenes_ganancia_edit', { 'id': 2 }) }}">
                      {% image 'assets/self/images/editar.png' %}
                        <img src="{{ asset_url }}" alt="Editar" />
                      {% endimage %}
          </a>
        </td>
      </tr>      
    </tbody>
</table>
</div>
{% endblock %}

{% block javascripts %}
  <script>
    var _globales = {
        titulosNoFiltrados: '[0,1,2]',
        idListado: 'system-parameters',
        urlDatatableLang: '{{ asset('lang.txt') }}'
  };
  </script>

  {% javascripts  'assets/self/js/general/general_functions.js' filter='?uglifyjs2' output='js/compiled/app.js' %}
    <script src="{{ asset_url }}"></script>
  {% endjavascripts %} 
{% endblock %}
